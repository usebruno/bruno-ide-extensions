import styled from 'styled-components';

const StyledWrapper = styled.div`
  .test-success {
    color: green;
  }

  .test-failure {
    color: red;
  }

  .error-message {
    color: gray;
  }
`;

export default StyledWrapper;
